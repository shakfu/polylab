# Building faust with LLVM support



If FAUST_DIR is where faust is git cloned, then to build faust with LLVM support:

1. Copy the `build.sh` to FAUST_DIR

2. Copy `CMakeLists.txt` to FAUST_DIR//embedded/faustgen

